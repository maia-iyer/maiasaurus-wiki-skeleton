# Phase 1: Fetch, Extract & Route (site mode)

## Fetch

Starting from the seed URL, crawl linked pages within the same domain. Cap at 20 pages. Store crawled content in a temporary working set.

## Extract metadata

- title (from site title/homepage), URL (seed), suggested key, one-liner, source type = `site`
- Note crawl date prominently

## Duplicate check

Same as single-page mode.

## Metadata table

Present metadata and confirm before proceeding.
